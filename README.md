# Survey React App

Aplicación web desarrollada en React + Vite que contiene pantallas de autenticación
(Login y Register) y un módulo de encuesta.

---

## 📦 Instalación de dependencias

Después de clonar el repositorio, ejecuta:

```bash
npm install
